interface GeneratedContentProps {
  generation: string;
}

const GeneratedContent = ({ generation }: GeneratedContentProps) => {
  return <div>{generation}</div>;
};

export default GeneratedContent;
